import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test placeholder', (WidgetTester tester) async {
    // Firebase 및 Daily.co 초기화가 필요하므로 통합 테스트로 진행 예정
    expect(true, isTrue);
  });
}
